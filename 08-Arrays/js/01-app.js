const numeros = [10, 20, 30];

// Sintaxis no tan común de crear arreglos
const meses = new Array("Enero", "Febrero", "Marzo");

console.log(numeros);
console.log(meses);

// Un arreglo que contiene datos de todo tipo
const deTodo = [
  "Hola",
  10,
  true,
  "si",
  null,
  {nombre: "Erik", trabajo: "Desarrollador .NET"},
  [1, 2, 3],
];
console.log(deTodo);
